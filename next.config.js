/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['onestaforms.com.pl', 'cdn.pixabay.com','images.pexels.com','res.cloudinary.com','cloudinary.com'],
  },
}

module.exports = nextConfig
